/*6-3
#include <stdio.h>
int main(void)
{
	char first = 'F';
	char outside,inside;
	
	for (outside = first;outside >= 'A';outside--)
	{
		for (inside = first;inside >= outside;inside--)
			{
				printf("%c",inside);
			}
		printf("\n");
	}
	return 0;
}*/

#include
